;-----------------------------------------------------------------------------------------------------------------------------------------------------
; Config
;-----------------------------------------------------------------------------------------------------------------------------------------------------

	print "----------------------------------------------------"
	print " config.asm"
	print "----------------------------------------------------"
	print

;-----------------------------------------------------------------------------------------------------------------------------------------------------
; default highscore table and settings
;-----------------------------------------------------------------------------------------------------------------------------------------------------

	org pageConfig

.config
	skip 0					; show this address in listing



;-----------------------------------------------------------------------------------------------------------------------------------------------------
; high score table
;-----------------------------------------------------------------------------------------------------------------------------------------------------

	equb &00,&45,&00			; 45000
	equs "UNIVERSAL*",&FF

	equb &00,&40,&00			; 40000
	equs "LADYBUG   ",&FF

	equb &00,&35,&00			; 35000
	equs "STARDOT   ",&FF

	equb &00,&30,&00			; 30000
	equs "LADYBUG   ",&FF

	equb &00,&25,&00			; 25000
	equs "STARDOT   ",&FF

	equb &00,&20,&00			; 20000
	equs "LADYBUG   ",&FF

	equb &00,&15,&00			; 15000
	equs "STARDOT   ",&FF

	equb &00,&10,&00			; 10000
	equs "LADYBUG   ",&FF




;-----------------------------------------------------------------------------------------------------------------------------------------------------
; settings					default settings
;-----------------------------------------------------------------------------------------------------------------------------------------------------

defaultLadybugLives	= 3			; these values are also used for a demo game instead of the player configured values
defaultEnemySpeed	= 1
defaultEnemyTarget	= 1

;-----------------------------------------------------------------------------------------------------------------------------------------------------

	equb defaultLadybugLives		; lady bug lives	1-9
	equb defaultEnemySpeed			; enemy speed		0-3 (0=easy, 1=normal, 2=hard, 3=max)
	equb defaultEnemyTarget			; enemy target (skill)	0-3 (0=easy, 1=normal, 2=hard, 3=max)
	equb 1					; timer volume		0-3 (0=off, 1=low, 2=medium, 3=high)
	equb 1					; sound enable		0-2 (0=off, 1=game sound on, 2=game and demo sound on)

	equb keyX				; right 'X'
	equb keyZ				; left 'Z'
	equb keySlash				; down '/'
	equb keyColon				; up ':'

	equs "XZ/:"				; ascii text for keys

	equb &00				; validation code, no need to calculate it here
						; it is calculated in game and by menu.bas, reset.bas



;-----------------------------------------------------------------------------------------------------------------------------------------------------
.configEnd
	skip 0					; show this address in listing
;-----------------------------------------------------------------------------------------------------------------------------------------------------

	print
	print
	print
